{
  "name": "protocol-chat-api",
  "version": "1.0.0",
  "description": "Backend proxy for Protocol AI chat; keeps Gemini API key server-side.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "optionalDependencies": {
    "dotenv": "^16.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
